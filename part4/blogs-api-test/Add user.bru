meta {
  name: Add user
  type: http
  seq: 4
}

post {
  url: http://localhost:3001/api/users
  body: json
  auth: inherit
}

body:json {
  {
    "username": "{{username}}",
    "password": "{{password}}",
    "name": "{{name}}"
  }
}

vars:pre-request {
  username: user
  password: Password123
  name: Just Some User
}

settings {
  encodeUrl: true
  timeout: 0
}
